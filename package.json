{
  "name": "pdf_reader",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "react-native start --reset-cache",
    "test": "jest",
    "lint": "eslint .",
    "dev": "react-native run-ios && react-native run-android",
    "cleanDev":"cd android && ./gradlew clean && cd .. && cd ios && pod install && cd .. && react-native run-ios && react-native run-android"
  },
  "dependencies": {
    "fbjs": "^0.8.18",
    "react": "17.0.2",
    "react-native": "0.66.1",
    "react-native-blob-util": "^0.13.16",
    "react-native-pdf": "^6.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "eslint": "7.14.0",
    "jest": "^26.6.3",
    "metro-react-native-babel-preset": "^0.66.2",
    "react-test-renderer": "17.0.2"
  },
  "jest": {
    "preset": "react-native"
  }
}
